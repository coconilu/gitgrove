# 方案：自研项目管理（本地优先）

> 状态：待 review。评审通过后按「分期路线」拆 tower 任务实施。
> 关联：#29（GitHub Projects 面板，未达预期）、#40（多级同步预留）

## 1. 背景：为什么放弃 GitHub Projects 路线

#29 交付了 GitHub Projects v2 只读面板，实测未达预期，根因是路线本身而非实现质量：

1. **数据模型受限**：字段、状态、看板列都必须是 GitHub 的模型，我们想做的「按本地仓库 / 分支 / checkout 聚合」这类视图它表达不了。
2. **授权摩擦**：project scope 需要单独申请 PAT / 追加授权，写操作还要更大权限。
3. **只读起步、写操作昂贵**：拖卡片改状态这种基本交互都要走 GraphQL mutation + 错误处理 + 乐观更新。
4. **GitGrove 的核心资产在本地**：本地仓库、分支、checkout、work item 是用户真正的「项目」维度，GitHub 对此一无所知。

结论：项目管理能力改为**自研、本地优先**，GitHub 退居「互通的数据源」而非「数据主人」。

## 2. 目标与非目标

**目标**

- GitGrove 内置项目管理：任务（item）的创建、编辑、流转、归档。
- 两个核心效率视图：**看板**（拖拽流转）与**里程碑**（进度聚合）。
- 与 GitGrove 现有能力深度集成：任务 ↔ 仓库 / 分支 / PR / checkout。
- 与 GitHub issue **双向互通**（不是只读镜像）。
- 零授权摩擦、零速率限制、离线可用。

**非目标（P0/P1 不做）**

- 多人实时协作、权限体系（→ #40 远期）。
- 移动端 / Web 端。
- 评论、附件、富文本（P2 再评估）。

## 3. 数据模型（为 #40 多级同步预留）

所有实体主键用 **ULID**（可排序、无中心发号），更新时间戳 + **软删除（tombstone）**，从第一天保证「可 diff、可合并」。

**Item（任务）**

| 字段 | 说明 |
| --- | --- |
| id | ULID |
| title / body | 标题 / 描述（markdown 纯文本） |
| status | 自定义状态机，默认 `backlog / todo / doing / done / archived`，用户可增删列 |
| priority | `none / low / medium / high / urgent` |
| milestoneId | 可空，归属里程碑 |
| labels | 字符串数组 |
| repoPath | 可空，关联本地仓库 |
| branch | 可空，关联分支（可一键创建） |
| githubRef | 可空，`{ owner, repo, number, nodeId, syncState }`，见 §6 |
| dueDate | 可空 |
| order | 看板列内排序（fractional indexing，避免重排全列） |
| createdAt / updatedAt / deletedAt | tombstone 预留 |

**Milestone（里程碑）**：id、title、description、dueDate、状态（open/closed）、进度由 items 聚合（done/total、按优先级加权可选）。

存储：**SQLite**（rusqlite，随 app 数据目录落盘），理由：查询/筛选/排序性能好、事务可靠、与 Tauri 集成成熟；同时提供**整库导出/导入 JSON** 命令，作为备份与 #40 同步的交换格式。

## 4. 核心视图

**看板（Kanban）**

- 列 = 自定义状态，拖拽卡片流转（乐观更新，本地写无延迟）。
- 顶部过滤条：按里程碑 / 仓库 / 标签 / 优先级筛选；搜索框。
- 卡片信息密度可调：紧凑（标题+优先级点）/ 完整（里程碑、仓库、PR 状态、截止日）。
- 列内拖拽排序即 `order` 字段，fractional indexing 只写一行。

**里程碑（Milestone）**

- 里程碑列表：进度条（done/total）、截止日倒计时、逾期高亮。
- 里程碑详情：内嵌过滤后的看板/列表视图。
- 「今日聚焦」：跨里程碑聚合 doing + 今日到期，作为可选默认首页。

（表格视图作为看板的备选渲染，复用同一套筛选，P1 做。）

## 5. 与 GitGrove 现有能力的集成

1. **任务 → 分支**：item 上「创建分支」按钮（`feat/<ulid前8位>-<slug>`），自动写入 item.branch；分支面板反向显示关联任务。
2. **任务卡片 → PR 状态**：branch 有开放 PR 时，卡片显示 PR 编号 + CI 状态点（复用 #30 的 GitHub 查询通道，仅展示，不做写）。
3. **merge 自动流转**：检测到关联 PR merged → item 建议流转到 done（toast 确认，不静默改）。
4. **仓库上下文**：从某个仓库/checkouts 进入项目页时，看板默认带上该仓库过滤。

## 6. GitHub issue 双向互通

- **拉入**：从已登录仓库的 issue 列表「导入为任务」，建立 `githubRef` 映射；标题/正文/标签/里程碑/issue state 进入本地字段。
- **推出**：本地任务可「创建为 issue」或「关联已有 issue」。
- **同步策略**：手动触发 + 定时（30min）双向；字段级规则——
  - title / labels / milestone：以**本地为准**推上去（本地是工作台）；
  - issue state：GitHub closed → 本地建议流转 done；本地 done → 可一键 close issue（不自动，避免误关）；
  - 冲突（两端同字段都变过，按 updatedAt 判断）：保留本地值并在卡片上标「与 GitHub 有分歧」，点开可选方向。
- 授权：复用现有 GitHub 登录态，issues scope 已在现有权限内，**无新增授权摩擦**。

## 7. 技术方案

- **后端**：`app/src-tauri/src/pm/` 新模块——`store.rs`（rusqlite + migration）、`model.rs`、`github_sync.rs`（复用 github.rs 的 gql/rest 通道）；Tauri commands 参照 projects_v2.rs 的分层。
- **前端**：Sidebar 新增「项目」页签（或复用 #29 的 Projects 入口改造），`app/src/components/pm/`；拖拽用 `@dnd-kit/core`（轻量、无布局约束），其余复用 #31 的 ui/ 组件库。
- **替换关系**：#29 的 Projects v2 面板（projects_v2.rs + ProjectsPanel）在 P0 落地后**退役删除**，代码量大但都是只读查询层，无业务依赖。
- **测试**：store 层 Rust 单测（migration、CRUD、排序）；sync 映射表驱动测试；前端沿用 node --test 体系测状态逻辑。

## 8. 分期路线

| 期 | 内容 | 验收 |
| --- | --- | --- |
| P0 | 数据模型 + SQLite store、看板视图（拖拽/筛选/自定义列）、里程碑列表与详情、#29 旧面板退役 | 本地建任务/拖卡/建里程碑全流程可用，重启不丢 |
| P1 | 分支关联与一键建分支、PR/CI 状态上卡、merge 建议流转、表格视图 | 任务-分支-PR 链路闭环 |
| P2 | GitHub issue 双向互通（导入/推出/同步）、JSON 导出导入 | issue ↔ 任务双向同步按 §6 规则工作 |
| 远期 | 多级同步（#40）、评论/附件 | 另行立项 |

P0/P1 不依赖 GitHub 任何东西；P2 才开始碰网络。

## 9. 风险与开放问题

1. **拖拽库选型**：@dnd-kit 增加 ~40KB，备选原生 HTML5 DnD（零依赖但移动端/键盘差）——P0 开工前定。
2. **看板默认列**：内置四列是否贴合你的习惯，review 时确认。
3. **#29 旧面板退役时机**：P0 可用后即删，避免双面板并存困惑；旧代码保留在 git 历史中。
4. **多级同步（#40）**：本方案已预留 ULID/tombstone/JSON 导出三个钩子，同步载体（git 仓/云盘/服务端）届时再决。
